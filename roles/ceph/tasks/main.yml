---
# Ceph role dispatcher — orchestration is in 03-configure-storage.yml,
# which imports specific tasks_from per play. This file exists for
# `roles: [ceph]` entry-point compatibility but is intentionally empty.

- name: Ceph role default entry-point is unused — see 03-configure-storage.yml
  ansible.builtin.debug:
    msg: >-
      Use `tasks_from:` to invoke specific stages
      (preflight, disks, install, init, mon-mgr, osd, pools, cephfs, rgw, users, output).
  run_once: true
