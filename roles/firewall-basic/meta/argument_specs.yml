---
argument_specs:
  main:
    short_description: Configure UFW firewall with inbound/outbound rules
    author:
      - Ivan Perdomo
    options:
      firewall_outbound_policy:
        type: str
        default: "allow"
        choices:
          - allow
          - deny
        description:
          - "Default policy for outgoing traffic."
          - "When 'deny', only ports in outbound_allow are permitted out."
      inbound_allow:
        type: list
        elements: dict
        default: []
        description:
          - "List of inbound ports to allow."
          - "Each item must have 'port' (int or range string) and 'proto' (tcp/udp)."
      outbound_allow:
        type: list
        elements: dict
        default: []
        description:
          - "List of outbound ports to explicitly allow."
          - "Useful when firewall_outbound_policy is 'deny'."
      outbound_deny:
        type: list
        elements: dict
        default: []
        description:
          - "List of outbound ports to explicitly deny."
          - "Applied regardless of firewall_outbound_policy."
