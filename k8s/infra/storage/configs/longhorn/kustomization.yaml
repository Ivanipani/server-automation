---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - backup-target.yaml
  - recurring-job.yaml
  - nodes.yaml
  # GCS bucket as a second BackupTarget (HMAC over S3 interop).
  - gcs-backup-target.yaml
  - gcs-s3-credentials.sops.yaml
