---
# On-demand PR verification. NOT reconciled by Flux — kick it by hand:
#   kubectl create -n tekton-ci -f runs/verify.run.yaml
#   # or with tkn:
#   tkn pipeline start verify -n tekton-ci \
#     -p revision=<sha> -w name=source,volumeClaimTemplateFile=... \
#     -w name=ssh-creds,secret=doghouse-git
apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  generateName: verify-
  namespace: tekton-ci
spec:
  taskRunTemplate:
    serviceAccountName: tekton-ci-bot
  pipelineRef:
    name: verify
  params:
    - name: revision
      value: main
  workspaces:
    - name: source
      volumeClaimTemplate:
        spec:
          # Pin Longhorn explicitly — the cluster currently has two default
          # StorageClasses (k3s local-path + longhorn), so an unset class binds
          # ambiguously.
          storageClassName: longhorn
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 4Gi
    - name: ssh-creds
      secret:
        secretName: doghouse-git
