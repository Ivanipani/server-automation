---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kube-prometheus-stack
  # NOTE: metallb + traefik moved to ../networking/configs (decoupled from
  # storage — see clusters/doghouse/networking.yaml).
