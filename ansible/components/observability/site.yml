---
# Component: observability (layer 2) — see component.yml / README.md.
#
# Apply:  just observability
#
# Day-2 service, order-independent: depends only on os-baseline having
# created the ansible user.

- name: Install Prometheus node_exporter
  hosts: "{{ observability_hosts | default('physical') }}"
  become: true
  roles:
    - role: node-exporter
      tags: [node-exporter]
