---
argument_specs:
  main:
    short_description: Install fwupd and ensure the LVFS remote is enabled
    author:
      - Ivan Perdomo
    options:
      firmware_enabled:
        type: bool
        default: true
        description: "False apt-removes fwupd (runs uninstall.yml) instead of installing it and enabling the LVFS remote (install.yml)."
