---
# Gated off in playbooks/poochella/infra/20-hypervisor/20-cluster.yml.
# Kept for reference; do not import from a live playbook.
#
# Establishes inter-node root SSH trust prerequisite for `pvecm add`,
# scoped to THIS host's cluster only (`groups[proxmox_cluster_name]`), so
# multiple clusters / standalone nodes don't cross-authorize.
# Idempotent: re-running on configured nodes is a no-op.

- name: Ensure /root/.ssh exists
  ansible.builtin.file:
    path: /root/.ssh
    state: directory
    owner: root
    group: root
    mode: "0700"

- name: Ensure root SSH keypair exists
  community.crypto.openssh_keypair:
    path: /root/.ssh/id_ed25519
    type: ed25519
    owner: root
    group: root
    mode: "0600"

- name: Read root public key
  ansible.builtin.slurp:
    src: /root/.ssh/id_ed25519.pub
  register: root_pubkey_slurp

- name: Expose root pubkey as fact
  ansible.builtin.set_fact:
    root_pubkey: "{{ root_pubkey_slurp.content | b64decode | trim }}"

- name: Authorize peer root pubkeys for cluster SSH
  ansible.posix.authorized_key:
    user: root
    key: "{{ hostvars[item].root_pubkey }}"
    state: present
  loop: "{{ groups[proxmox_cluster_name] }}"

- name: Ensure /etc/hosts entries for cluster peers
  ansible.builtin.blockinfile:
    path: /etc/hosts
    marker: "# {mark} ANSIBLE MANAGED: pve-cluster"
    block: |
      {% for h in (groups[proxmox_cluster_name] | sort) %}
      {{ hostvars[h].ip_address }} {{ h }}
      {% endfor %}

- name: Configure root SSH client for cluster peers
  ansible.builtin.blockinfile:
    path: /root/.ssh/config
    create: true
    owner: root
    group: root
    mode: "0600"
    marker: "# {mark} ANSIBLE MANAGED: pve-cluster"
    block: |
      {% for h in (groups[proxmox_cluster_name] | sort) %}
      Host {{ h }} {{ hostvars[h].ip_address }}
          StrictHostKeyChecking accept-new
          UserKnownHostsFile /root/.ssh/known_hosts
      {% endfor %}
