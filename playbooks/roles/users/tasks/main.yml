---
- name: Create promoter group
  ansible.builtin.group:
    name: promoter
    state: present

- name: Create talent group
  ansible.builtin.group:
    name: talent
    state: present

- name: Create Ivan user
  ansible.builtin.include_tasks: ivan-dev.yml
